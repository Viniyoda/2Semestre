namespace exercicio1
{
    //Escreva um programa TypeScript que imprima todos os números primos de 1 a 53 usando a função while.

    let numero: number = 0;
    console.log('Os números primos enter 1 e 53 são:')
    while (numero = (numero*1)) {
        console.log(`${numero}`);
        numero += 1;
        if (numero = 53)
    }
}